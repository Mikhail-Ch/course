<%
const cfg = App.GetConfig( "./data.json" );
const {
	extraClasses,
	extraAttrs
} = cfg
%>

<%
const classes = `header ${extraClasses.join(" ")}`.trim()
const attrs = extraAttrs.join(" ")
%>

<header
	class="<%= classes %>"
	<%- extraAttrs %>
>
	HEADER
</header>
