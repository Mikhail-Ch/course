<%
const cfg = App.GetConfig( "./data.json" );
const {
	extraClasses,
	extraAttrs
} = cfg
%>

<%
const classes = `footer ${extraClasses.join(" ")}`.trim()
const attrs = extraAttrs.join(" ")
%>

<footer
	class="<%= classes %>"
	<%- extraAttrs %>
>
	FOOTER
</footer>
